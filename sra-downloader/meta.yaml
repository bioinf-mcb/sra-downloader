{% set name = "sra-downloader" %}
{% set version = "1.0.3" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: ead4d21c391863f47ca5e0e3e84c39b0de565f3b52e3785711f0bccb41f25043

build:
  number: 0
  entry_points:
    - sra-downloader=sra_downloader.__main__:main
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - pip
    - python
  run:
    - python
    - sra-tools >=2.9.6

test:
  imports:
    - sra_downloader
  commands:
    - sra-downloader --help

about:
  home: "https://github.com/bioinf-mcb/sra-downloader"
  license: MIT
  license_family: MIT
  license_file: 
  summary: "A script for batch-downloading and automatic compression of data from NCBI Sequence Read Archive. Built on SRA-Toolkit."
  doc_url: 
  dev_url: 

extra:
  recipe-maintainers:
    - wwydmanski
